{Application 'MERGETEAMS' logic file generated by CSPro}
PROC GLOBAL

PROC CLUSTERS_FF

PROC CLUST_LEVEL
XGRAPPE = YIDZD;
if loadcase(EQUIPE_GRAPPE_DICT, XGRAPPE) then
  YEQCART = XEQUIPE;
else
  errmsg("cluster %d,%d not found)", YCLUSTER, YIDZD);
endif;
